// todo remove this file after fetching data from API

const data = [];

for (let i = 0; i < 60; i++) {
  data.push({
    key: `${i}`,
    name: `${i}`,
    chinese: `${i}`,
    math: ` ${i}` ,
    english: ` ${i}`,
  })
}


export default data;